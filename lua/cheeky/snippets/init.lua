local all = require("cheeky.snippets.all")
local lua = require("cheeky.snippets.lua")
local cpp = require("cheeky.snippets.cpp")
local asm = require("cheeky.snippets.asm")

return {
  all = all,
  lua = lua,
  cpp = cpp,
  asm = asm
}
